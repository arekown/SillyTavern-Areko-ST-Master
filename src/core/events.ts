// TODO: Auto-Mode verdrahten.
// off      -> keine Listener
// inputs   -> bei USER_MESSAGE_RENDERED
// responses-> bei CHARACTER_MESSAGE_RENDERED
// both     -> beide
// Timing AFTER nutzt diese Events; Timing BEFORE laeuft ueber den Interceptor.
export function wireAutoMode(): void {
  // wird in Phase 2 implementiert
}
