// TODO: buildPrompt() + Generator -> generateTracker(messageId).
// Nutzt das gewaehlte Connection-Profil und das aktive Preset-Schema.
export async function generateTracker(_messageId: number): Promise<void> {
  // wird in Phase 2 implementiert
}
